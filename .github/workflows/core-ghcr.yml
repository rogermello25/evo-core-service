name: Build & Publish Core Service

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - 'evo-ai-core-service-community/**'
      - '.github/workflows/core-ghcr.yml'

permissions:
  contents: read
  packages: write

env:
  REGISTRY: ghcr.io/rogermello25/evocrm
  IMAGE_NAME: evo-core-fixed

jobs:
  build:
    name: Build
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ secrets.GHCR_USERNAME }}
          password: ${{ secrets.GHCR_TOKEN }}

      - name: Build and push
        uses: docker/build-push-action@v6
        with:
          context: ./evo-ai-core-service-community
          file: ./evo-ai-core-service-community/Dockerfile
          platforms: linux/amd64
          push: true
          provenance: false
          tags: |
            ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:latest
            ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${{ github.sha }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Image digest
        run: echo "Built image digest"
